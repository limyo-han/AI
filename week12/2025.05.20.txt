시험범위는 1장부터 12장까지, 13~16장은 필요한 것만

feature engineering에서 기존의 특성을 가지고 새로운 특성을 만드는 것에는 한계가 있다(다양성과 추세성 부족)

F1 Score를 쓰는 이유
-> imbalanced data 고려

macro avg - F1 Score 평균
weighted avg 가중치(데이터 개수) 고려한 F1 Score 평균

weight = weight - learning rate*gradient

p173 표5-2 계산해보기

p187 계산문제

퍼셉트론에서 activation 함수 -> 계단 함수.
계단 함수 - 0보다 작으면 0, 0보다 크면 1로 출력

활성화 함수를 사용하는 이유
층 사이사이에 비선형성을 부가함으로써 더 다양한 특징을 추출

p243 문제 풀어보기


케라스를 이용하는 3가지 방법

1. Sequential 모델을 만들고 필요한 레이어를 추가하는 방법
2. 함수형 API를 사용하는 방법
3. Model 클래스를 상속받아서 정의하는 방법

*****그래디언트 소실 문제
심층 신경망에서 그래디언트가 전달되다가 점점 0에 가까워지는 현상
왜? 언제? 어떻게 해결?

softmax 활성화 함수
다중 클래스 분류에 사용, 각 클래스에 대한 확률 분포를 만들어줌

*****드롭아웃
과적합은 은닉층의 노드가 너무 많아져도 발생


pretrained model이 무엇인가